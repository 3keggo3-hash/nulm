# API Rules

- Keep MCP surface, tool implementations, config/state, and workflow layers separate.
- Preserve public tool schemas unless the task explicitly changes the API.
- Update docs/tests when user-facing behavior changes.
