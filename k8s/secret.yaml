apiVersion: v1
kind: Secret
metadata:
  name: nulm-secret
  labels:
    app: nulm
    app.kubernetes.io/name: nulm
    app.kubernetes.io/component: core
type: Opaque
data:
  # NOTE: In production, use ESO, Sealed Secrets, or external secret management
  # Values below are base64-encoded placeholders - replace before deployment
  otlp-endpoint: <BASE64_PLACEHOLDER_REPLACE_ME>
  ai-evaluator-api-key: <BASE64_PLACEHOLDER_REPLACE_ME>